# Plugin foundation evidence

See the planning research captured 2026-07-24:

- Internal `DocumentFilter` / `FilterRegistry` and pipeline `StepRegistry` exist.
- No public plugin runtime, SDK package, `plugin.*` protocol, or lifecycle store.
- Desktop Engine client already proves newline JSON-RPC process supervision.
- Parent design specifies three tiers and local distribution first.
- Recommended MVP: Tier 3 process filter vertical slice with hello-srt example.
